//Use map to create an array of the length of each country name in the countries array.
const countries = ['Pakistan', 'Turkey', 'Saudi Arabia', 'Iran', 'Afghanistan']
const cntry=countries.map((c)=>c.length)
console.log(cntry);